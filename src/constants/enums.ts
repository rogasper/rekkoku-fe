// User roles enum
export enum Role {
  USER = "USER",
  ADMIN = "ADMIN",
}

// Post status enum (from Prisma schema)
export enum PostStatus {
  DRAFT = "DRAFT",
  PUBLISHED = "PUBLISHED",
  ARCHIVED = "ARCHIVED",
}
